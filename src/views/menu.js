/* export const menu = () => {
  const navMenu = document.createElement('nav');

  const viewMenu = ` 
          <ul>Nuevo Post</ul>
          <ul>Buscar</ul>
          <ul>Cerrar Sesión</ul>
     `;
  navMenu.innerHTML = viewMenu;

  return navMenu;
}; */
